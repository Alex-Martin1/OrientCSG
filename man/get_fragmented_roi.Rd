% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragmented-roi.R
\name{get_fragmented_roi}
\alias{get_fragmented_roi}
\title{Copy a 3D Slicer ROI generator for fragmented solid meshes}
\usage{
get_fragmented_roi(
  model_name,
  limits = c(20, 80),
  transverse_margin_mm = 2,
  create_la_line = FALSE
)
}
\arguments{
\item{model_name}{Character scalar giving the expected 3D Slicer model name.}

\item{limits}{Numeric vector of length two giving the lower and upper
longitudinal ROI limits as percentages of \code{LongMax}. Defaults to
\code{c(20, 80)}.}

\item{transverse_margin_mm}{Non-negative numeric scalar giving the additional
transverse ROI margin, in millimetres, added on each side. Defaults to \code{2}.}

\item{create_la_line}{Logical. If \code{TRUE}, the generated Slicer code also
creates a markup line joining the two \code{LongMax} endpoints. Defaults to
\code{FALSE}.}
}
\value{
Invisibly returns \code{TRUE} if the generated Python block was copied to
the clipboard successfully. If no supported clipboard mechanism is
available, a warning is issued and \code{FALSE} is returned invisibly.
}
\description{
\code{get_fragmented_roi()} generates a 3D Slicer Python block for quickly
creating an oriented ROI around a selected longitudinal portion of a surface
mesh and copies that block directly to the system clipboard.
}
\details{
The longitudinal reference used by this helper is \code{LongMax}, calculated from
the two mesh vertices with the greatest Euclidean distance. This is a
geometric approximation of longitudinal direction and is not the anatomical
or biomechanical length calculated by \code{\link[=orient_longbone]{orient_longbone()}}. The ROI is intended
as a convenient starting point for manual cropping in 3D Slicer.

The generated Slicer code first looks for a valid model whose name exactly
matches \code{model_name}. If no exact match is found and only one valid model is
present, that model is used with a warning in the Slicer Python console. If
several valid models are present, Slicer opens an interactive model selector.
}
\examples{
\dontrun{
get_fragmented_roi("W30")
get_fragmented_roi("W30", limits = c(10, 90), create_la_line = TRUE)
}

}
