% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{get_tcl}
\alias{get_tcl}
\title{Extract Avizo TCL commands from an orientation result}
\usage{
get_tcl(res, section = NULL)
}
\arguments{
\item{res}{An orientation result returned by \code{\link[=orient_mandible]{orient_mandible()}} or
\code{\link[=orient_longbone]{orient_longbone()}}.}

\item{section}{Optional section name. For mandibular results, expected values
are usually \code{"CS1"}, \code{"CS2"}, or \code{"CS3"}. For long-bone results, expected
values follow the format \code{"SECTION_35"}, \code{"SECTION_50"}, and so on.}
}
\value{
A character scalar containing one or more Avizo TCL command blocks.
}
\description{
\code{get_tcl()} retrieves the Avizo TCL command block generated by
\code{\link[=orient_mandible]{orient_mandible()}} or \code{\link[=orient_longbone]{orient_longbone()}}. The block can then be pasted into
the Avizo/Amira console to orient the relevant Slice and Clipping Plane
objects automatically.
}
\details{
If \code{section} is \code{NULL}, all available TCL blocks are returned as one character
string separated by visual dividers. If \code{section} is specified, only the block
for that section is returned.
}
\examples{
\dontrun{
tcl_cs1 <- get_tcl(res, section = "CS1")
cat(tcl_cs1)
}

}
